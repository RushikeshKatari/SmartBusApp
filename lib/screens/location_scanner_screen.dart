import 'package:flutter/material.dart'; import 'package:provider/provider.dart'; import '../providers/smart_bus_provider.dart'; import '../theme/app_theme.dart'; import '../widgets/location_sharing_widgets.dart'; import 'location_permission_screen.dart';
class LocationScannerScreen extends StatelessWidget {const LocationScannerScreen({super.key});@override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text('Share bus location',style:TextStyle(fontWeight:FontWeight.w800))),body:Padding(padding:const EdgeInsets.all(20),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('Scan bus QR',style:TextStyle(fontSize:27,fontWeight:FontWeight.w800)),const SizedBox(height:6),const Text('Scan the QR inside the bus to start sharing its location.',style:TextStyle(color:AppColors.muted)),const SizedBox(height:28),const ModernQRScannerOverlay(),const Spacer(),ModernPrimaryButton(label:'Simulate secure QR scan',icon:Icons.qr_code_rounded,onPressed:(){context.read<SmartBusProvider>().verifyLocationQr();Navigator.pushReplacement(context,MaterialPageRoute(builder:(_)=>const LocationPermissionScreen()));}),const SizedBox(height:8),const Center(child:Text('Only one device can share a bus location at a time.',style:TextStyle(color:AppColors.muted,fontSize:12)))])));}
