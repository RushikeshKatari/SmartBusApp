/// Replace the visual map placeholder with GoogleMap when platform API keys are
/// configured. No map or backend connection is made by this mock frontend.
abstract final class MockMapConfig {
  static const defaultZoom = 15.0;
}
